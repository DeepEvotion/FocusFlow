# 🔄 Обновление GitHub репозитория с сохранением бекапа

## 📋 Стратегия обновления

Мы обновим репозиторий https://github.com/DeepEvotion/FocusFlow.git, сохранив старую версию в отдельной ветке.

---

## 🛡️ Шаг 1: Создание бекапа старой версии

### 1.1 Клонируйте существующий репозиторий (если еще не сделано)

```bash
# Если у вас еще нет локальной копии
git clone https://github.com/DeepEvotion/FocusFlow.git focusflow-old
cd focusflow-old
```

### 1.2 Создайте ветку бекапа

```bash
# Создайте ветку с текущей версией
git checkout -b backup-old-version

# Отправьте бекап на GitHub
git push origin backup-old-version
```

Теперь старая версия сохранена в ветке `backup-old-version` ✅

---

## 🚀 Шаг 2: Подготовка новой версии

### 2.1 Перейдите в папку с новой версией

```bash
# Перейдите в папку с текущим проектом (новая версия)
cd C:\Users\Admin\Desktop\FocusFlow
```

### 2.2 Инициализируйте Git (если еще не сделано)

```bash
# Проверьте, есть ли уже Git
git status

# Если Git не инициализирован:
git init
```

### 2.3 Подключите удаленный репозиторий

```bash
# Добавьте удаленный репозиторий
git remote add origin https://github.com/DeepEvotion/FocusFlow.git

# Или если уже добавлен, обновите URL:
git remote set-url origin https://github.com/DeepEvotion/FocusFlow.git
```

---

## 📦 Шаг 3: Создание коммита новой версии

### 3.1 Проверьте, что исключено из Git

```bash
# Убедитесь, что .env не будет закоммичен
git status

# Проверьте .gitignore
cat .gitignore
```

### 3.2 Добавьте все файлы

```bash
git add .
```

### 3.3 Создайте коммит

```bash
git commit -m "feat: major update - FocusFlow v2.0

✨ Новые функции:
- Изумрудная тема (черно-зеленая) с анимациями
- Кнопки перемотки музыки на ±5 секунд
- Улучшенный дизайн плеера
- Стилизованная кнопка Google OAuth
- Обновленная документация

🎨 Дизайн:
- Все синие цвета заменены на изумрудные
- Анимация изменения цвета текста в кнопках
- Эффекты свечения и туманностей
- Плавные переходы

🐛 Исправления:
- Прогресс-бар загрузки на Яндекс.Диск
- Цвет никнейма пользователя
- Цвета панели плеера

📚 Документация:
- Обновлен README.md
- Добавлен CONTRIBUTING.md
- Добавлены GitHub шаблоны"
```

---

## 🔄 Шаг 4: Обновление GitHub (с сохранением истории)

### Вариант A: Мягкое обновление (рекомендуется)

Этот способ сохраняет всю историю коммитов.

```bash
# Получите последние изменения из GitHub
git fetch origin

# Объедините с историей (разрешите несвязанные истории)
git pull origin main --allow-unrelated-histories

# Если возникнут конфликты, разрешите их:
# 1. Откройте конфликтующие файлы
# 2. Выберите нужную версию (обычно новую)
# 3. Добавьте разрешенные файлы:
git add .
git commit -m "merge: resolve conflicts with old version"

# Отправьте изменения
git push origin main
```

### Вариант B: Полная замена (если не нужна старая история)

⚠️ **Внимание**: Этот способ удалит всю историю коммитов в main ветке!

```bash
# Создайте новую ветку
git checkout -b main-new

# Принудительно отправьте на GitHub
git push origin main-new:main --force
```

---

## ✅ Шаг 5: Проверка

### 5.1 Проверьте GitHub

1. Откройте https://github.com/DeepEvotion/FocusFlow
2. Убедитесь, что новая версия загружена
3. Проверьте, что ветка `backup-old-version` существует

### 5.2 Проверьте ветки

```bash
# Посмотрите все ветки
git branch -a

# Должны быть:
# * main (новая версия)
# remotes/origin/backup-old-version (старая версия)
```

---

## 🎯 Рекомендуемая последовательность действий

### Полная инструкция:

```bash
# 1. Создайте бекап старой версии (если еще не сделано)
cd /path/to/old/focusflow
git checkout -b backup-old-version
git push origin backup-old-version

# 2. Перейдите в новую версию
cd C:\Users\Admin\Desktop\FocusFlow

# 3. Инициализируйте Git (если нужно)
git init

# 4. Подключите репозиторий
git remote add origin https://github.com/DeepEvotion/FocusFlow.git

# 5. Создайте коммит
git add .
git commit -m "feat: major update - FocusFlow v2.0"

# 6. Получите старую версию и объедините
git fetch origin
git pull origin main --allow-unrelated-histories

# 7. Разрешите конфликты (если есть)
# Выберите новую версию для всех файлов
git add .
git commit -m "merge: update to v2.0"

# 8. Отправьте на GitHub
git push origin main
```

---

## 🔍 Проверка безопасности

### Перед отправкой убедитесь:

```bash
# Проверьте, что .env не в Git
git ls-files | grep "\.env$"
# Должно быть пусто

# Проверьте, что база данных не в Git
git ls-files | grep "\.db$"
# Должно быть пусто

# Проверьте размер репозитория
git count-objects -vH
# Должно быть < 50 МБ
```

---

## 🆘 Если что-то пошло не так

### Откат изменений

```bash
# Если еще не отправили на GitHub:
git reset --hard HEAD~1

# Если уже отправили:
git revert HEAD
git push origin main
```

### Восстановление из бекапа

```bash
# Переключитесь на бекап
git checkout backup-old-version

# Создайте новую ветку из бекапа
git checkout -b main-restored

# Отправьте на GitHub
git push origin main-restored:main --force
```

---

## 📞 Доступ к старой версии

После обновления старая версия будет доступна:

1. **Через ветку**: https://github.com/DeepEvotion/FocusFlow/tree/backup-old-version
2. **Через клонирование**:
   ```bash
   git clone -b backup-old-version https://github.com/DeepEvotion/FocusFlow.git
   ```

---

## ✨ После успешного обновления

### Обновите README.md

Замените в README.md:
- `your-username` → `DeepEvotion`
- Добавьте скриншоты (если есть)

### Создайте Release

1. Перейдите в Releases на GitHub
2. Нажмите "Create a new release"
3. Tag: `v2.0.0`
4. Title: `FocusFlow v2.0.0 - Major Update`
5. Описание: скопируйте из коммита
6. Нажмите "Publish release"

### Обновите описание репозитория

1. Settings → General
2. Description: `🚀 Современное веб-приложение для продуктивности и управления временем`
3. Topics: `productivity`, `pomodoro`, `task-management`, `flask`, `python`, `sqlite`

---

<p align="center">
  <strong>Готово! Ваш репозиторий обновлен с сохранением бекапа 🎉</strong>
</p>
